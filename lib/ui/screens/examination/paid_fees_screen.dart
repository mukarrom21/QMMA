import 'package:flutter/material.dart';

class PaidFeesScreen extends StatelessWidget {
  const PaidFeesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Paid Fees'),
      ),
    );
  }
}
