import 'package:flutter/material.dart';

class ExamNameScreen extends StatelessWidget {
  const ExamNameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Exam Name'),
    );
  }
}
