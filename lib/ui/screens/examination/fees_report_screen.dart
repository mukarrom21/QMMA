import 'package:flutter/material.dart';

class FeesReportScreen extends StatelessWidget {
  const FeesReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Fees Report'),
      ),
      body: const Center(
        child: Text('Fees Report'),
      ),
    );
  }
}
