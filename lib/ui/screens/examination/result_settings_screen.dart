import 'package:flutter/material.dart';

class ResultSettingsScreen extends StatelessWidget {
  const ResultSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Result Settings'),
    );
  }
}
