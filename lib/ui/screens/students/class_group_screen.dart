import 'package:flutter/material.dart';

class ClassGroupScreen extends StatelessWidget {
  const ClassGroupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Class Group Screen'),
    );
  }
}
